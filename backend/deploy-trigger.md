# Deploy trigger (backend)

Archivo dummy para forzar un redeploy del backend en Railway. Seguro de borrar.
No afecta el build de Gradle.

- 2026-06-19: redeploy con el SDK de Sentry + `SENTRY_DSN` (proyecto java-backend).
